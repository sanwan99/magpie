import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("Magpie")
            .font(.system(size: 24, weight: .bold))
            .padding()
    }
}

#Preview {
    ContentView()
}
